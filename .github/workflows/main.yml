name: Metrics
on:
  # Schedule daily updates
  schedule: [{cron: "0 0 * * *"}]
  # (optional) Run workflow manually
  workflow_dispatch:
  # (optional) Run workflow when pushing on master/main
  push: {branches: ["main"]}
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    environment:
      name: production
    permissions:
      contents: write
    steps:
      # ==========================================================
      # WORKAROUND: config_display: columns is broken upstream
      # (lowlighter/metrics#839, discussion #803) — height doesn't
      # respond to the @media breakpoint so it renders single-column
      # unpredictably. Split into two files + <img align> in README
      # instead. If they ever fix it, merge these two steps back into
      # one with `config_display: columns` and drop the align hack
      # in the README.
      # ==========================================================

      # LEFT COLUMN — header, activity, calendar
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          user: gamer0kayf1n3
          filename: metrics-left.svg
          template: classic
          # config_display: columns  # <- re-enable here when fixed, merge with right column config below
          base: header, activity, community
          plugin_introduction: yes
          # plugin_achievements: no # fine we're disabling it grr
          # plugin_achievements_display: compact
          # plugin_achievements_only: polyglot, stargazer, sponsor, deployer, member, maintainer, developer, scripter, packager, explorer, infographile # not all for now, screw you github sunset api changes
          plugin_activity_types: PushEvent, PullRequestEvent, IssuesEvent, IssueCommentEvent, CreateEvent # not all for now
          plugin_calendar: yes
          plugin_calendar_limit: 0
          plugin_activity: no # fine we're disabling it grr
          plugin_activity_limit: 5
          plugin_activity_days: 0
          plugin_isocalendar: yes
          # removed habits for now
          config_timezone: Asia/Manila

      # RIGHT COLUMN — repositories, languages, stars, traffic, code, leetcode
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          user: gamer0kayf1n3
          filename: metrics-right.svg
          template: classic
          base: repositories, metadata
          plugin_code: yes
          plugin_code_load: 400
          plugin_code_days: 365
          plugin_code_languages: javascript, typescript, python, html, css, java, astro, cpp
          plugin_languages: yes # i will cry if i have to disavle this too
          plugin_languages_ignored: >-
            tex, less, dockerfile, makefile, qmake, lex, cmake, shell,
            gnuplot
          plugin_languages_indepth: yes
          plugin_languages_details: lines, bytes-size
          plugin_languages_limit: 4
          plugin_languages_analysis_timeout: 15
          plugin_stars: yes
          plugin_stars_limit: 3
          plugin_traffic: yes
          plugin_leetcode: yes
          plugin_leetcode_sections: solved, skills, recent
          config_timezone: Asia/Manila
